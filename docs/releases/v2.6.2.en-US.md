### Added

- Released LauncherGo 2.6.2 and updated the default local development version.

### Fixed

- Fixed mod ZIP paths containing spaces being split into invalid paths, which caused imports to report that no mod with `modinfo.json` was found.
- Mod paths selected through the file picker are now consistently quoted, and a single existing path pasted without quotes is also supported.
